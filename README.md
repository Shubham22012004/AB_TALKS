This project uses the Stack data structure to validate whether a bracket sequence is balanced correctly.

Features------------------

Supports multiple brackets: (), {}, []
Detects invalid or mismatched brackets
Demonstrates stack operations (push and pop)

How It Works--------------

Opening brackets are pushed onto the stack
Closing brackets check the top of the stack
If all brackets match correctly and the stack becomes empty, the sequence is valid